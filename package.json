{
  "name": "proyecto-noche",
  "private": true,
  "version": "1.1.4",
  "description": "Noite: actividades, planes y recuerdos en tu computadora.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@proyecto-noche/desktop",
    "build": "npm run build --workspace=@proyecto-noche/domain && npm run build --workspace=@proyecto-noche/desktop",
    "tauri": "npm run tauri --workspace=@proyecto-noche/desktop --",
    "lint": "npm run lint --workspace=@proyecto-noche/desktop",
    "test": "npm run test --workspace=@proyecto-noche/desktop"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=18.18"
  }
}
